const React = require('react')

const Friend = React.createClass({
    render() {
        return (
            <div>
                <h1>Friend show page</h1>
            </div>
        )
    }
})

module.exports = Friend
