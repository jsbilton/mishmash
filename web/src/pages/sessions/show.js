const React = require('react')

const Session = React.createClass({
    render() {
        return (
            <div>
                <h1>New Session Show Page</h1>
            </div>
        )
    }
})

module.exports = Session
