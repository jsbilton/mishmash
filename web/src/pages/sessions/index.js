const React = require('react')

const Sessions = React.createClass({
    render() {
        return (
            <div>
                <h1>Sessions List
                </h1>
            </div>
        )
    }
})

module.exports = Sessions
